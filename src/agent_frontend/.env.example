# Base URL of the C# backend the SPA calls directly from the browser (no proxy tier).
# Local dev: point at the locally running backend. In Azure this is baked at build time from the
# VITE_AGENT_BACKEND_URL provision output (the Static Web App's build reads it via azd).
VITE_AGENT_BACKEND_URL=http://localhost:8000
